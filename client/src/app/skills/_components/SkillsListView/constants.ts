/** Constants for SkillsListView. */

/** Responsive card grid — same track sizing as the agents grid. */
export const CARD_GRID_COLS = "repeat(auto-fill, minmax(280px, 1fr))";

/** Preview drawer width (px). */
export const PREVIEW_WIDTH = 520;
