/* /skills — Skills grid (A1, L02). Page stays thin; the view owns the logic. */
import { SkillsListView } from "./_components/SkillsListView/SkillsListView";

export default function SkillsPage() {
  return <SkillsListView />;
}
